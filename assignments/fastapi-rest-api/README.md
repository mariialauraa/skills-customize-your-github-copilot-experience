# 📘 Assignment: Building REST APIs with FastAPI

## 🎯 Objective

Students will learn how to build RESTful APIs using the FastAPI framework. This assignment will cover creating endpoints, handling requests, and returning responses in JSON format.

## 📝 Tasks

### 🛠️ Task 1: Setup FastAPI Project

#### Description
Set up a new FastAPI project with the necessary dependencies and folder structure.

#### Requirements
Completed program should:

- Include a `main.py` file as the entry point.
- Install FastAPI and Uvicorn as dependencies.
- Run the server locally using Uvicorn.

### 🛠️ Task 2: Create API Endpoints

#### Description
Create endpoints for a simple CRUD application (e.g., managing a list of tasks).

#### Requirements
Completed program should:

- Include endpoints for creating, reading, updating, and deleting tasks.
- Use appropriate HTTP methods (GET, POST, PUT, DELETE).
- Return responses in JSON format.

### 🛠️ Task 3: Add Input Validation

#### Description
Use Pydantic models to validate input data for the API endpoints.

#### Requirements
Completed program should:

- Define Pydantic models for request and response schemas.
- Validate input data and return appropriate error messages for invalid data.